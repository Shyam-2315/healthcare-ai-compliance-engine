services:
  ai-service:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8001:8001"
    volumes:
      - ai-temp-uploads:/tmp/healthcare-compliance-ai
    restart: unless-stopped

volumes:
  ai-temp-uploads:
